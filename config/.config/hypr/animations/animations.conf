
# https://wiki.hypr.land/Configuring/Variables/#animations
# =================================================
# HYPRLAND ANIMATIONS — RED TEAM PROFILE
# =================================================
animations {
    enabled = yes

<<<<<<< HEAD
    # -----------------------------------------------------
    # Curves (custom tuned)
    # -----------------------------------------------------
    # NAME            X0     Y0     X1     Y1
    bezier = snap,        0.15, 0.85, 0.25, 1
    bezier = oversnap,    0.12, 0.90, 0.18, 1.10
    bezier = smoothfast,  0.30, 0.00, 0.20, 1
    bezier = microbounce, 0.20, 1.30, 0.30, 1
    bezier = linear,      0.00, 0.00, 1.00, 1.00

    # -----------------------------------------------------
    # Global
    # -----------------------------------------------------
    animation = global, 1, 10, default

    # -----------------------------------------------------
    # Windows (main character)
    # -----------------------------------------------------
    animation = windows,    1, 3.2, snap
    animation = windowsIn,  1, 3.6, oversnap, popin 85%
    animation = windowsOut, 1, 2.4, snap,     popin 90%

    # -----------------------------------------------------
    # Borders (subtle polish)
    # -----------------------------------------------------
    animation = border, 1, 2.5, smoothfast

    # -----------------------------------------------------
    # Fades (clean & quick)
    # -----------------------------------------------------
    animation = fadeIn,  1, 1.8, smoothfast
    animation = fadeOut, 1, 1.4, smoothfast
    animation = fade,    1, 2.2, smoothfast

    # -----------------------------------------------------
    # Layers (menus, rofi, notifications)
    # -----------------------------------------------------
    animation = layersIn,  1, 3.2, snap, fade
    animation = layersOut, 1, 2.2, snap, fade
    animation = fadeLayersIn,  1, 1.6, smoothfast
    animation = fadeLayersOut, 1, 1.2, smoothfast

    # -----------------------------------------------------
    # Workspaces (unique feel)
    # -----------------------------------------------------
    animation = workspaces,    1, 2.6, microbounce, slide
    animation = workspacesIn,  1, 2.4, snap, fade
    animation = workspacesOut, 1, 2.4, snap, fade

    # -----------------------------------------------------
    # Zoom (optional, but smooth)
    # -----------------------------------------------------
    animation = zoomFactor, 1, 4.5, smoothfast
=======
    # =================================================
    # CURVES 
    # =================================================
    #        NAME,              X0,   Y0,   X1,   Y1
    bezier = snapOut,          0.15, 1.0,  0.30, 1.0
    bezier = snapIn,           0.70, 0.00, 0.20, 1.0
    bezier = smoothOps,        0.25, 1.0,  0.35, 1.0
    bezier = linearOps,        0.00, 0.00, 1.00, 1.00
    bezier = microFade,        0.40, 0.00, 0.60, 1.00
    bezier = aggressive,       0.10, 0.00, 0.10, 1.00

    # =================================================
    # GLOBAL
    # =================================================
    animation = global,        1, 8, smoothOps

    # =================================================
    # WINDOWS 
    # =================================================
    animation = windows,       1, 4.5, smoothOps
    animation = windowsIn,     1, 4.0, snapOut, popin 88%
    animation = windowsOut,    1, 2.0, linearOps, popin 88%

    # =================================================
    # BORDERS  
	# =================================================
    animation = border,        1, 5.0, snapOut

    # =================================================
    # FADING 
	# =================================================
    animation = fade,          1, 2.5, microFade
    animation = fadeIn,        1, 1.4, microFade
    animation = fadeOut,       1, 1.2, microFade

    # =================================================
    # LAYERS
    # =================================================
    animation = layers,        1, 3.2, smoothOps
    animation = layersIn,      1, 3.5, snapOut, fade
    animation = layersOut,     1, 1.6, linearOps, fade
    animation = fadeLayersIn,  1, 1.6, microFade
    animation = fadeLayersOut, 1, 1.3, microFade

    # =================================================
    # WORKSPACES 
    # =================================================
    animation = workspaces,    1, 1.4, aggressive, slide
    animation = workspacesIn,  1, 1.0, aggressive, slide
    animation = workspacesOut, 1, 1.2, aggressive, slide

    # =================================================
    # ZOOM
    # =================================================
    animation = zoomFactor,    1, 6.0, snapOut
>>>>>>> ab0a0e0ed29173775964f80a3f99dec53cb59eb9
}
